import React from "react";
import { LoaderContainer, Spinner } from "./styled";

const Loader = () => {
    return (
        <LoaderContainer>
            <Spinner />
        </LoaderContainer>
    );
};

export default Loader;
