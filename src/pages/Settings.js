import React from "react";
import { NavbarTitle } from "./styled";

const Settings = () => {
    return (
        <NavbarTitle>
            <h2>Settings</h2>
        </NavbarTitle>
    );
};

export default Settings;
